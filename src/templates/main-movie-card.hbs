{{#each this}}
<li class="card">
  <div class="card-thumb">
    <img src={{poster_path}} alt={{original_title}} class="card-image">
  </div>
  <div class="card-content">
    <h2 class="card-title">{{original_title}}</h2>
    <class="card-description">{{overview}} | {{release_date}}</p>
  </div>
</li>
{{/each}}